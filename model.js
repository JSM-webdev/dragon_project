const model = []
const dragonTypes = ['fire', 'water', 'earth', 'air']
dragonFactory(type)
}

increment(timestamp) {
    const target = dragons.find(dragon => dragon.timestamp === timestamp)
    target.clicks += 1
    rerender()
}
